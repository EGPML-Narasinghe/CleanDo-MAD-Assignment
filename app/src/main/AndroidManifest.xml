<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cleando">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/Theme.Test">
        <activity android:name="com.example.cleando.admin_order_stuff"></activity>
        <activity android:name="com.example.cleando.rejected_orders" />
        <activity android:name="com.example.cleando.confirmed_order" />
        <activity android:name="com.example.cleando.admin_order" />
        <activity android:name="com.example.cleando.client.client_cart" />
        <activity android:name="com.example.cleando.client.client_order" />
        <activity android:name="com.example.cleando.client.client_dashboard" />
        <activity android:name="com.example.cleando.products" />
        <activity android:name="com.example.cleando.add_products" />
        <activity android:name="com.example.cleando.admin" />
        <activity android:name="com.example.cleando.signup" />
        <activity
            android:name="com.example.cleando.login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>